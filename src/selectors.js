export const SEL = {
  promptTextarea: 'textarea',
  generateBtn: 'button:has-text("Generate"), button[aria-label*="Generate"]',
  resultGallery: '[data-testid*="result"], [class*="result"]',
  resultTiles: 'img, picture img, [role="img"] img',
  ratioDropdown: '[aria-label*="Aspect"], [data-testid*="aspect"]',
  styleDropdown: '[aria-label*="Style"], [data-testid*="style"]',
};
